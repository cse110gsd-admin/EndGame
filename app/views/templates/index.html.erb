<h1>Listing templates</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Comments</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @templates.each do |template| %>
  <tr>
    <td><%= template.name %></td>
    <td><%= template.description %></td>
    <td><%= link_to 'View', template_path(template) %></td>
    <td><%= link_to 'Edit', edit_template_path(template) %></td>
    <!--td><%= link_to 'Destroy', template, :confirm => 'Are you sure?', :controller => "template", :method => :delete %></td-->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Named Workout', new_template_path %>
